this is ruby implementation of Unbabel.

= requirements

* Ruby >= 1.8.x
* RSpec (gem install rspec) to run tests

= how to run tests

 spec spec/spec_unbabel.rb
 spec spec/spec_*

 ruby -Ilib example/fib_scheme.rb

= todo

* Tempfileを作るのをやめて、Popen3を使うようにする
* Signeture を Signature に直す(^^;
